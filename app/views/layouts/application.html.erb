<!DOCTYPE html>
<html>
  <head>
    <title>ElTraining</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <div class='app-title navbar navbar-expand-md navbar-light bg-light mb-5'>
      <div class='navbar-brand'>
        タスクマネージャ
      </div>
      <ul class='navbar-nav ml-auto'>
        <li class='nav-item'><%= link_to '一覧', tasks_path, class: 'nav-link' %></li>
        <li class='nav-item'><%= link_to '作成', new_task_path, class: 'nav-link' %></li>
      </ul>
    </div>
    <div class='container'>
      <div class='notification'>
        <% flash.each do |key, value| %>
          <%= content_tag :div, value, class: key %>
        <% end %>
      </div>
      <%= yield %>
    </div>
  </body>
</html>
