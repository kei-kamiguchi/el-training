<h1>新規登録</h1>
<%= form_with model: @task, local: true do |form| %>
  <%= form.label :title %>
  <%= form.text_field :title %>
  <%= form.label :content %>
  <%= form.text_area :content %>
  <%= form.submit nil %>
<% end %>
