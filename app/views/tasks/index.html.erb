<h1>タスク一覧</h1>
<% @tasks.each do |task| %>
  <%= link_to task.title, task_path(task), name: '詳細' %>
  <%= task.content %>
  <%= link_to '削除', task, method: :delete %>
  <%= link_to '編集', edit_task_path(task) %>
<% end %>
